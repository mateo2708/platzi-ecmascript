const array = ['one', 'two', 'three', 'four', 'five', 'six'];

// Before at()
console.log(array[array.length - 1]);

// After at() the function can be used to get a defined position using a relative position
console.log(array.at(-1));
